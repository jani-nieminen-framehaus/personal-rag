# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual envs
.venv/
venv/
env/

# Editor
.idea/
.vscode/
*.swp
.DS_Store

# Eval runs (keep golden_set.jsonl; ignore ad-hoc reports)
eval/runs/
*.eval.json

# Logs
*.log
.venv_tests/

# Metadata store (runtime artifact; recreate via `rag ingest` / `rag ask`)
metadata.sqlite3*

# Smoke / debug artifacts
test_out.pdf
samples/test_books/


# One-off scratch/debug scripts (never commit)
/_*.py

# Golden sets over the real corpus — personal data, never committed
eval/golden_candidates.jsonl
eval/golden_real.jsonl
